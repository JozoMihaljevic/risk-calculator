import { SamobrojeviDirective } from './samobrojevi.directive';

describe('SamobrojeviDirective', () => {
  it('should create an instance', () => {
    const directive = new SamobrojeviDirective();
    expect(directive).toBeTruthy();
  });
});
